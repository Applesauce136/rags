#lang racket

(require "contracts.rkt")

(provide
 translate
 scale
 rotate
 )

(define scale
  (lambda (pts sx sy sz)
    '()))

(define translate
  (lambda (pts tx ty tz)
    '()))

(define rotate
  (lambda (pts axis angle)
    '()))
