#lang racket

(require "contracts.rkt")

(provide
 translate
 scale
 rotate
 )

(define translate
  (lambda (pts tx ty tz)
    '()))

(define scale
  (lambda (pts sx sy sz)
    '()))

(define rotate
  (lambda (pts axis angle)
    '()))
